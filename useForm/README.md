# 💻 use formHook

Ejemplo de uso:
```
   const [ fomrValues, handleChange, reset ] = useForm({
      {
      name: '',
      email: '',
      password: '',
   }
   });

```

```
const { name, email, password } = formValues;

```